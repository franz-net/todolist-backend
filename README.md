# todolist-backend

My first attempt at creating a backend REST API with Flask

## Roadmap

-   Implement "addTask"
-   Add a backend DB to hold the task
-   Add `due date` field
-   Add `user` fields
